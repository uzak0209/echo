type Post {
  id: ID!
  content: String!
  imageUrl: String
}

type Query {
  timeline(limit: Int!): [Post!]!
}

type Mutation {
  createPost(content: String!, imageUrl: String): Boolean!
  incrementDisplayCount(postId: Int!): Boolean!
}
